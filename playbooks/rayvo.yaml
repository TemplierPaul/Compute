inventory:
  hosts: hosts.yaml
  configs: configs.yaml

variables:
  "£hosts": 
    - pando
    # - calmip
  "£atari":   "--job=$SLURM_JOB_ID --net=conv --episode_frames=27000 --stack_frames=4 --max_frames=1000000000 --max_evals=10000000"
  "£minatar": "--job=$SLURM_JOB_ID --net=min --mu=12 --episode_frames=27000 --max_frames=100000000 --max_evals=10000000"
  "£mujoco":  "--job=$SLURM_JOB_ID --mu=12 --episode_frames=1000 --max_frames=100000000 --max_evals=10000000"
  "£procgen": "--net=impala --episode_frames=1000 --stack_frames=1 --max_frames=250000000 --max_evals=10000000 --job=$SLURM_JOB_ID"

tasks:
  # - name: "Git pull"
  #   hosts:
  #     - pando
  #     - calmip
  #   git:
  #     action: pull
  #     path: "~/Rayvolution"

  # - name: "Run Atari"
  #   hosts:
  #     # - calmip
  #     - pando
  #   slurm:
  #     job_name: "Atari"
  #     cmd: "python3 Rayvolution/simple.py £atari --pop=24 --mu=12 --max_frames=1000000000 --max_evals=10000000"
  #     config: sepcma
  #     ray: true
  #     scatter: true
  #     seeds: 0
  #     sweep:
  #       "--optim": ["canonical", "sepcma"]
  #     args:
  #       - "--env=Pong-v0"
  #       - "--env=Alien-v0"

  - name: "Run Procgen"
    hosts:
      # - calmip
      - pando
    slurm:
      job_name: "Procgen"
      cmd: "python3 Rayvolution/simple.py £procgen --pop=48 --mu=12 --tag=procgen_branch"
      config: sepcma
      ray: true
      scatter: false
      seeds: 0
      sweep:
        "--optim": 
          - sepcma
          - canonical
        "--seeding": 
          - random
          - unique
          - generation
        "--wandb": ["sureli/rayvolution"]
      args:
        - "--env=bigfish-easy"
        - "--env=bossfight-easy"
        - "--env=starpilot-easy"
        - "--env=leaper-easy"
